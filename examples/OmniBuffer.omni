ins 2:
    "buffer"
    "speed"  {1, 0, 10}

outs 1

init:
    buffer = Buffer(input_num = 1)
    phase = 0.0

perform:
    scaled_rate = buffer.samplerate / samplerate
    
    sample:
        out1 = buffer[phase]
        phase += (in2 * scaled_rate)
        phase = phase % buffer.len
